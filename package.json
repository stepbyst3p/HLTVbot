{
  "name": "hltvbot",
  "version": "1.0.0",
  "description": "bot for monitoring upcoming matches, results and streams from HLTV.org",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spike",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.24.0",
    "cheerio": "^0.22.0",
    "hltv": "^0.2.1",
    "request": "^2.79.0"
  }
}
